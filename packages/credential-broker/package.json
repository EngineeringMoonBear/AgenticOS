{
  "name": "@agenticos/credential-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ADR-0001 caching credential broker: holds one read-only, vault-scoped 1Password service-account token and serves allowlisted secrets from an in-memory cache.",
  "main": "src/main.mjs",
  "scripts": {
    "start": "node src/main.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@1password/sdk": "^0.3.1"
  },
  "engines": {
    "node": ">=22"
  }
}
