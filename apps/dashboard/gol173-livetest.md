# GOL-173 live test — agent PR review pipeline

Throwaway file to fire a pull_request event that matches the frontend review glob (apps/dashboard/**).
Validates the H1 fan-out fix (PR #287) routes pull_request into handlePrInbound.
Safe to delete; PR will be closed after the review pipeline fires.
